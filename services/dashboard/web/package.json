{
  "name": "@plynf/dashboard-web",
  "version": "0.1.0",
  "description": "Plynf dashboard SPA — Vite + Preact frontend, served by the dashboard FastAPI service",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "preact": "^10.22.0",
    "preact-router": "^4.1.2",
    "@preact/signals": "^1.3.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.2",
    "@testing-library/preact": "^3.2.4",
    "@types/node": "^20.14.0",
    "happy-dom": "^14.12.0",
    "typescript": "^5.4.5",
    "vite": "^5.3.0",
    "vitest": "^1.6.0"
  }
}
